#include <stdio.h>

/**
 * main - entry point
 * Description: a program that prints its name, followed by a new line
 * @argc: argument count
 * @argv: argument vector
 * Return: prints program name
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
